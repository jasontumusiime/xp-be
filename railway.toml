[build]
builder = "nixpacks"

[deploy]
startCommand = "alembic upgrade head && python app/initial_data.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/v1/utils/health-check/"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
